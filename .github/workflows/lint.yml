name: Lint & Test

on:
  workflow_dispatch: {}
  workflow_call:
    inputs:
      run_crawl_test:
        description: Run live crawl integration test (release and other callers)
        type: boolean
        default: false
  push:
    branches:
      - master
      - dev
    paths:
      - "lncrawl/**"
      - "sources/**/*.py"
      - "pyproject.toml"
      - "uv.lock"
      - ".github/workflows/lint.yml"
  pull_request:
    branches:
      - dev
    paths:
      - "lncrawl/**"
      - "sources/**/*.py"
      - "pyproject.toml"
      - "uv.lock"
      - ".github/workflows/lint.yml"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.9", "3.10", "3.11", "3.12", "3.13", "3.14"]
    name: Python ${{ matrix.python-version }}
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v6
        with:
          submodules: recursive

      - name: 🐍 Install uv & Python ${{ matrix.python-version }}
        uses: astral-sh/setup-uv@v7
        with:
          python-version: ${{ matrix.python-version }}
          enable-cache: true

      - name: 📦 Install Dependencies
        run: uv sync --frozen --extra dev

      - name: 🧹 Lint Code
        run: |
          uv run ruff check
          uv run ruff format --check

      - name: 🏗️ Build Wheel
        run: uv run python -m build --wheel --no-isolation

      - name: 🧪 Test Installation
        run: pip install dist/lightnovel_crawler*.whl

      - name: 🧪 Check lncrawl
        run: lncrawl -ll sources list

      - name: 🧪 Test Crawl
        if: ${{ github.event_name == 'workflow_dispatch' || inputs.run_crawl_test == true }}
        run: lncrawl -ll crawl 'https://novelfull.com/the-kings-avatar-for-the-glory.html' --first 2 --format epub --noin
